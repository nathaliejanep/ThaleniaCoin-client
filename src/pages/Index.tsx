import React from 'react';
import { useAuth } from '../hooks/AuthProvider';

const Index: React.FC = () => {
  // const { isAuthenticated } = useAuth();
  // console.log(isAuthenticated);
  return <div>Index Welcome to</div>;
};

export default Index;
